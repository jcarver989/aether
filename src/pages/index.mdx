---
layout: ../layouts/Landing.astro
title: "Aether - A modular AI agent framework for Rust"
description: "A modular, open-source AI agent framework written in Rust."
---

import { Code } from "astro-expressive-code/components";
import Hero from "../components/Hero.astro";
import FeatureStep from "../components/FeatureStep.astro";
import FeatureItem from "../components/FeatureItem.astro";
import ToolShowcaseItem from "../components/ToolShowcaseItem.astro";
import PackageGrid from "../components/PackageGrid.astro";
import PackageCard from "../components/PackageCard.astro";
import CallToAction from "../components/CallToAction.astro";

export const step1Code = `{
  "agents": [
    {
      "name": "Frontier",
      "model": "codex:gpt-5.4"
    },
    {
      "name": "Smart",
      "model": "zai:glm-5"
    },
    {
      "name": "Fast",
      "model": "openrouter:minimax/minimax-m2.5"
    }
  ]
}`;

export const step2aCode = `{
  "servers": {
    "coding": { "type": "in-memory" }
  }
}`;

export const step2bCode = `{
  "servers": {
    "coding": { "type": "in-memory" },
    "skills": { "type": "in-memory" }
  }
}`;

export const step2cCode = `{
  "servers": {
    "coding":    { "type": "in-memory" },
    "skills":    { "type": "in-memory" },
    "subagents": { "type": "in-memory" }
  }
}`;

export const step2dCode = `{
  "servers": {
    "coding":    { "type": "in-memory" },
    "skills":    { "type": "in-memory" },
    "subagents": { "type": "in-memory" },
    "proxy": {
      "type": "in-memory",
      "input": {
        "servers": {
          "chrome-devtools": {
            "type": "stdio",
            "command": "npx",
            "args": ["chrome-devtools-mcp"]
          }
        }
      }
    }
  }
}`;

export const step3aCode = `{
  "prompts": ["SYSTEM.md", "AGENTS.md"]
}`;

export const step3SubAgentCode = `{
  "agents": [
    {
        "name": "Explorer",
        "description": "Explores codebases to find relevant files, patterns, and integration points",
        "prompts": [".agents/explorer/AGENTS.md"],
    }
  ]
}`;

export const step3bCode = `# System Prompt
You are an expert senior Rust engineer.

## Coding Style
- Prefer generics over dynamic boxing
- Use async fn in traits directly
...`;

export const step3cCode = `# Agents
Use the codebase-explorer agent to
search code before making changes.
Use the Frontier agent for complex
multi-file refactors.
...`;

export const step4Code = `# Interactive TUI
$ aether

# Expose over ACP for editor/IDE integration
$ aether acp

# Headless mode for scripts & CI
$ aether headless -m codex:gpt-5.4 "refactor auth"`;

<Hero />

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="flex flex-col gap-16">
            <FeatureStep number="1" title="Control every token in context" subtitle="Aether agents start as blank slates, with no system prompt or tools. You have full control.">
                <div slot="code" class="flex flex-col gap-2">
                    <Code code={step3aCode} lang="json" title=".aether/settings.json" meta="ins={2}" />
                    <Code code={step3SubAgentCode} lang="json" title=".aether/settings.json" meta="ins={6}" />
                    <Code code={step3cCode} lang="md" title=".aether/prompts/AGENTS.md" />
                </div>
                <FeatureItem title="Load any file as a prompt" description="CLAUDE.md, AGENTS.md, **/*.md -- whatever you want." />
                <FeatureItem title="Globs are supported too" description="Package domain knowledge into skill directories with progressive disclosure. Primary SKILL.md loads first, auxiliary files load on demand &mdash; keeping context lean." />
                <FeatureItem title="Global and per-agent" description="Define custom commands as markdown files with YAML frontmatter. Restrict allowed tools per command, add argument hints, and share them across your team." border={false} />
            </FeatureStep>

            <FeatureStep number="2" title="Use any LLM" subtitle="Anthropic, OpenAI, DeepSeek, Ollama, and more. One unified interface, swap with a single line.">
                <Code slot="code" code={step1Code} lang="json" title=".aether/settings.json" meta="ins={2-15}" />
                <FeatureItem title="Cloud" description="Swap between 100+ providers with a single line. Anthropic, OpenAI, OpenRouter, DeepSeek, Bedrock, Ollama &mdash; all through one unified trait." />
                <FeatureItem title="Local" description="Alloy multiple models into a single agent. Round-robin across providers per turn &mdash; combine a reasoning model with a fast model to balance cost and capability." />
                <FeatureItem title="Bring your own" description="Configurable reasoning effort per agent &mdash; Low, Medium, or High. Extended thinking support for models that offer it. Cycle reasoning levels on the fly from the TUI." border={false} />
            </FeatureStep>

            <div>
                <div class="mb-6">
                    <h3 class="font-mono text-2xl sm:text-3xl font-semibold text-accent-500 mb-2">3. Use any tools</h3>
                    <p class="text-gray-400 text-sm sm:text-base leading-relaxed">Aether agents get tools exclusively via MCP.</p>
                </div>
                <div class="flex flex-col gap-6">
                    <ToolShowcaseItem title="Coding" description="File I/O, regex grep, bash execution, web fetch, and full LSP integration &mdash; go-to-definition, find references, rename symbols, and compiler diagnostics out of the box.">
                        <Code code={step2aCode} lang="json" title="mcp.json" meta="ins={3}" />
                    </ToolShowcaseItem>
                    <ToolShowcaseItem title="Skills" description="Reusable domain knowledge packaged as skill directories with progressive disclosure. Define slash commands as markdown with YAML frontmatter. Topic-based notes for learning consolidation.">
                        <Code code={step2bCode} lang="json" title="mcp.json" meta="ins={4}" />
                    </ToolShowcaseItem>
                    <ToolShowcaseItem title="Sub-agents" description="Spawn child agents that execute in parallel. Each gets its own model, prompts, and tools. Decompose tasks across multiple agents, then collect structured results when they all complete.">
                        <Code code={step2cCode} lang="json" title="mcp.json" meta="ins={5}" />
                    </ToolShowcaseItem>
                    <ToolShowcaseItem title="Proxy" description="Plug in any MCP-compatible server &mdash; Chrome DevTools, Playwright, databases, or your own custom tools. The proxy wraps external stdio servers into the unified in-memory interface." border={false}>
                        <Code code={step2dCode} lang="json" title="mcp.json" meta="ins={6-17}" />
                    </ToolShowcaseItem>
                </div>
            </div>

            <FeatureStep number="4" title="Run anywhere" subtitle="Interactive TUI, headless CLI for scripts and CI, or ACP server for editor integration.">
                <Code slot="code" code={step4Code} lang="sh" title="terminal" />
                <FeatureItem title="Interactive TUI" description="Rich terminal UI with markdown rendering, syntax highlighting, inline diffs, and live tool status. Switch agents, cycle reasoning levels, and pick files or commands inline." />
                <FeatureItem title="Headless CLI" description="Single-prompt mode for scripts and CI. Pipe input, choose output format (text, JSON, pretty), and run inside Docker sandboxes for isolation." />
                <FeatureItem title="ACP server" description="Expose your agent over the Agent Client Protocol for editor integration. Multi-session support, session persistence, and bi-directional streaming &mdash; works with Zed and any ACP client." border={false} />
            </FeatureStep>
        </div>
    </div>
</section>

<PackageGrid title="Modular by design" subtitle="Each package does one thing well. Compose only what you need.">
    <PackageCard name="aether-core" description="Agent runtime, context management, and the main conversation loop." />
    <PackageCard name="llm" description="Unified provider interface for Anthropic, OpenAI, Bedrock, Ollama, and more." />
    <PackageCard name="mcp-servers" description="Built-in MCP tool servers: file I/O, bash, web fetch, LSP integration." />
    <PackageCard name="wisp" description="Terminal UI with markdown, syntax highlighting, diffs, and session management." />
    <PackageCard name="crucible" description="Evaluation framework for testing agent behavior with assertions and metrics." />
    <PackageCard name="aether-cli" description="Command-line interface with headless mode, ACP relay, and session persistence." />
</PackageGrid>

<CallToAction title="Ready to build?" description="Get started with Aether in minutes. Read the docs, clone the repo, and start building your own AI agents.">
    <a href="/guides/introduction/" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium bg-accent-600 text-white hover:bg-accent-500 transition-all duration-200 shadow-lg shadow-accent-900/30">Read the docs <span aria-hidden="true">&rarr;</span></a>
    <a href="https://github.com/joshka/aether" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium border border-gray-500 text-gray-200 hover:border-accent-400 hover:text-accent-400 transition-all duration-200"><span>View on GitHub</span><svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg></a>
</CallToAction>
