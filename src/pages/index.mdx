---
layout: ../layouts/Landing.astro
title: "Aether - A modular AI agent framework for Rust"
description: "A modular, open-source AI agent framework written in Rust."
---

import { Code } from "astro-expressive-code/components";
import Hero from "../components/Hero.astro";
import ToolShowcaseItem from "../components/ToolShowcaseItem.astro";
import CallToAction from "../components/CallToAction.astro";
import TerminalDemo from "../components/TerminalDemo.astro";


export const installCode = `$ cargo install aether-cli`;
export const initCode = `$ aether init
✓ Created .aether/settings.json
✓ Created mcp.json
✓ Created AGENTS.md`;

export const showPromptCode = `$ aether show-prompt -a Smart

---
Prompt chars:            0
Tool schema chars:       0
Est. tokens:     ~       0
MCP tools:               0`;

export const promptsCode = `{
  "agents": [
    {
      "name": "Smart",
      "model": "zai:glm-5",
      "reasoningEffort": "high",
      "prompts": ["SYSTEM.md", "AGENTS.md"]
    }
  ]
}`;

export const systemPromptCode = `# System Prompt
You are an expert senior Rust engineer.
...`;

export const agentsPromptCode = `# Agents
Use the Explorer agent to search code.
...`;

export const showPromptCmd = `$ aether show-prompt -a Smart`;

export const showPromptOutput = `# System Prompt
You are an expert senior Rust engineer.
...

# Agents
Use the Explorer agent to search code.
...

---
Prompt chars:          120
Tool schema chars:       0
Est. tokens:     ~      30
MCP tools:               0`;

export const llmCloudCode = `{
  "agents": [
    {
      "name": "Smart",
      "model": "zai:glm-5",
      "reasoningEffort": "high"
    }
  ]
}`;

export const llmLocalCode = `{
  "agents": [
    {
      "name": "Smart",
      "model": "zai:glm-5",
      "reasoningEffort": "high"
    },
    {
      "name": "Local",
      "model": "llamacpp:qwen3"
    }
  ]
}`;

export const llmAlloyCode = `{
  "agents": [
    {
      "name": "Smart",
      "model": "zai:glm-5",
      "reasoningEffort": "high"
    },
    {
      "name": "Local",
      "model": "llamacpp:qwen3"
    },
    {
      "name": "Alloy",
      "model": "zai:glm-5,llamacpp:qwen3"
    }
  ]
}`;

export const toolsWireCode = `{
  "agents": [
    {
      "name": "Smart",
      "model": "zai:glm-5",
      "reasoningEffort": "high",
      "prompts": ["SYSTEM.md", "AGENTS.md"],
      "mcpServers": "mcp.json"
    }
  ]
}`;

export const toolsCodingCode = `{
  "servers": {
    "coding": { "type": "in-memory" }
  }
}`;

export const toolsSkillsCode = `{
  "servers": {
    "coding": { "type": "in-memory" },
    "skills": { "type": "in-memory" }
  }
}`;

export const toolsSubagentsCode = `{
  "servers": {
    "coding":    { "type": "in-memory" },
    "skills":    { "type": "in-memory" },
    "subagents": { "type": "in-memory" }
  }
}`;

export const toolsProxyCode = `{
  "servers": {
    "coding":    { "type": "in-memory" },
    "skills":    { "type": "in-memory" },
    "subagents": { "type": "in-memory" },
    "proxy": {
      "type": "in-memory",
      "input": {
        "servers": {
          "chrome-devtools": {
            "type": "stdio",
            "command": "npx",
            "args": ["chrome-devtools-mcp"]
          }
        }
      }
    }
  }
}`;

{/* ── Run anywhere ── */}

export const runTuiCode = `$ aether`;

export const runHeadlessCode = `$ aether headless -a Smart "refactor auth"`;

export const runAcpCode = `$ aether acp`;


<Hero />

<hr class="border-gray-700/50 mx-16" />

<section class="py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Getting started is easy as `install`, `init`, `run`</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">From install to running agent &mdash; everything is plain config files and markdown.</p>
        </div>
        <div class="flex flex-col gap-16">
            <ToolShowcaseItem title="1. Install" description="Install the CLI from crates.io.">
                <Code code={installCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="2. Init" description="Scaffold config files, tools, and a default agent prompt.">
                <Code code={initCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="3. Run" description="Launch the interactive TUI, run headless for scripts and CI, or expose over ACP for editor integration." border={false}>
                <Code code={runTuiCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Use any LLM, local or remote</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Anthropic, OpenAI, OpenRouter, DeepSeek, Moonshot, Zai, LlamaCpp and more. Plus, you can bring your own.</p>
        </div>
        <TerminalDemo id="llm-player" src="/demos/llm-demo.cast" class="mx-auto max-w-4xl mb-12" />
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Cloud" description="100+ cloud providers through one unified interface &mdash; OpenAI, Anthropic, DeepSeek, OpenRouter, Bedrock, and more. Configurable reasoning effort per agent.">
                <Code code={llmCloudCode} lang="json" title=".aether/settings.json" meta="ins={3-7}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Local" description="Run fully offline with llama.cpp, Ollama, or any local inference server. Same config format, no code changes.">
                <Code code={llmLocalCode} lang="json" title=".aether/settings.json" meta="ins={8-11}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Alloyed" description="Comma-separate models to create an alloy &mdash; requests round-robin across providers each turn. Mix cloud reasoning with local speed in a single agent." border={false}>
                <Code code={llmAlloyCode} lang="json" title=".aether/settings.json" meta="ins={12-15}" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Build a system prompt</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Start with a blank slate and add only what you need.</p>
        </div>
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Start with a blank slate" description="Agents begin with no system prompt or tools.">
                <Code code={showPromptCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Compose your system prompt" description="Add a prompts array to pull in markdown files &mdash; system instructions, agent guides, whatever you want.">
                <Code code={promptsCode} lang="json" title=".aether/settings.json" meta="ins={7}" />
                <Code code={systemPromptCode} lang="md" title="SYSTEM.md" />
                <Code code={agentsPromptCode} lang="md" title="AGENTS.md" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Measure" description="So you don't waste tokens." border={false}>
                <Code code={showPromptCmd} lang="sh" title="terminal" />
                <Code code={showPromptOutput} lang="md" title="output" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Connect tools</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Aether agents get tools exclusively via MCP. Add what you need, nothing more.</p>
        </div>
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Coding" description="Point the agent at an MCP config, then add servers. Coding gives you file I/O, regex grep, bash execution, web fetch, and full LSP integration out of the box.">
                <Code code={toolsWireCode} lang="json" title=".aether/settings.json" meta="ins={8}" />
                <Code code={toolsCodingCode} lang="json" title="mcp.json" meta="ins={3}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Skills" description="Reusable domain knowledge packaged as skill directories with progressive disclosure. Define slash commands as markdown with YAML frontmatter.">
                <Code code={toolsSkillsCode} lang="json" title="mcp.json" meta="ins={4}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Sub-agents" description="Spawn child agents that execute in parallel. Each gets its own model, prompts, and tools. Decompose tasks across multiple agents, then collect structured results.">
                <Code code={toolsSubagentsCode} lang="json" title="mcp.json" meta="ins={5}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Proxy" description="Plug in any external MCP server without bloating context. The proxy replaces all nested tool schemas with a single routing tool and writes definitions to disk &mdash; the agent browses them via the filesystem instead. 100 tools in context becomes 1, saving 90%+ on schema tokens every turn." border={false}>
                <Code code={toolsProxyCode} lang="json" title="mcp.json" meta="ins={6-17}" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Run anywhere</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Interactive TUI, headless CLI for scripts and CI, or ACP server for editor integration.</p>
        </div>
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Interactive TUI" description="Rich terminal UI with markdown rendering, syntax highlighting, inline diffs, and live tool status. Switch agents, cycle reasoning levels, and pick files or commands inline.">
                <Code code={runTuiCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Headless CLI" description="Single-prompt mode for scripts and CI. Pipe input, choose output format (text, JSON, pretty), and run inside Docker sandboxes for isolation.">
                <Code code={runHeadlessCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="ACP server" description="Expose your agent over the Agent Client Protocol for editor integration. Multi-session support, session persistence, and bi-directional streaming &mdash; works with Zed and any ACP client." border={false}>
                <Code code={runAcpCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<CallToAction title="Ready to build?" description="Use the full CLI or pick individual crates &mdash; aether-core, aether-llm, aether-mcp-servers, aether-wisp, aether-crucible &mdash; to compose your own agent.">
    <a href="/getting-started/introduction/" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium bg-accent-600 text-white hover:bg-accent-500 transition-all duration-200 shadow-lg shadow-accent-900/30">Read the docs <span aria-hidden="true">&rarr;</span></a>
    <a href="https://github.com/joshka/aether" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium border border-gray-500 text-gray-200 hover:border-accent-400 hover:text-accent-400 transition-all duration-200"><span>View on GitHub</span><svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg></a>
</CallToAction>
