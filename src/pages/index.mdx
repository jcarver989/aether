---
layout: ../layouts/Landing.astro
title: "Aether - A modular AI agent framework for Rust"
description: "A modular, open-source AI agent framework written in Rust."
---

import { Code } from "astro-expressive-code/components";
import Hero from "../components/Hero.astro";
import ToolShowcaseItem from "../components/ToolShowcaseItem.astro";
import CallToAction from "../components/CallToAction.astro";
import TerminalDemo from "../components/TerminalDemo.astro";
import { GITHUB_URL } from "../consts";


export const installCode = `$ cargo install aether-agent-cli`;
export const initCode = `$ aether agent new
✓ Created .aether/settings.json
✓ Created .aether/mcp.json
✓ Created .aether/SYSTEM.md
✓ Created AGENTS.md`;

export const showPromptCode = `$ aether show-prompt -a Build

---
Prompt chars:            0
Tool schema chars:       0
Est. tokens:     ~       0
MCP tools:               0`;

export const promptsCode = `{
  "agents": [
    {
      "name": "Build",
      "model": "zai:glm-5.1",
      "reasoningEffort": "high",
      "prompts": ["SYSTEM.md", "AGENTS.md"]
    }
  ]
}`;

export const systemPromptCode = `# System Prompt
You are an expert senior Rust engineer.
...`;

export const agentsPromptCode = `# Agents
Use the Explorer agent to search code.
...`;

export const showPromptCmd = `$ aether show-prompt -a Build`;

export const showPromptOutput = `# System Prompt
You are an expert senior Rust engineer.
...

# Agents
Use the Explorer agent to search code.
...

---
Prompt chars:          120
Tool schema chars:       0
Est. tokens:     ~      30
MCP tools:               0`;

export const llmCloudCode = `{
  "agents": [
    {
      "name": "Plan",
      "model": "codex:gpt-5.4",
      "reasoningEffort": "high"
    },
    {
      "name": "Build",
      "model": "zai:glm-5.1"
    },
    {
      "name": "Fast",
      "model": "openrouter:minimax/minimax-m2.7"
    }
  ]
}`;

export const llmLocalCode = `{
  "agents": [
    ...
    {
      "name": "Local",
      "model": "llamacpp:unsloth/Qwen3.5-9B-GGUF"
    }
  ]
}`;

export const llmAlloyCode = `{
  "agents": [
    ...
    {
      "name": "Alloy",
      "model": "zai:glm-5.1,llamacpp:unsloth/Qwen3.5-9B-GGUF"
    }
  ]
}`;

export const toolsWireCode = `{
  "agents": [
    {
      "name": "Build",
      "model": "zai:glm-5.1",
      "prompts": ["SYSTEM.md", "AGENTS.md"],
      "mcpServers": "mcp.json"
    }
  ]
}`;

export const toolsBuiltinCode = `{
  "servers": {
    "coding":    { "type": "in-memory" },
    "lsp":       { "type": "in-memory" },
    "skills":    { "type": "in-memory" },
    "subagents": { "type": "in-memory" },
    "tasks":     { "type": "in-memory" },
    "survey":    { "type": "in-memory" },
    "proxy":     { "type": "in-memory", "input": { ... } }
  }
}`;

export const toolsExternalCode = `{
  "servers": {
    ...
    "linear": {
      "type": "http",
      "url": "https://mcp.linear.app/mcp"
    }
  }
}`;

{/* ── Run anywhere ── */}

export const runTuiCode = `$ aether`;
export const runHeadlessCode = `$ aether headless "refactor auth"`;
export const runAcpCode = `$ aether acp`;


<Hero />

<hr class="border-gray-700/50 mx-16" />

<section class="py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Get up and running in ~5 minutes</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">From install to running agent &mdash; everything is plain config files and markdown.</p>
        </div>
        <div class="flex flex-col gap-16">
            <ToolShowcaseItem title="1. Install" description="Install the CLI from crates.io.">
                <Code code={installCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="2. Create your 1st agent" description="Scaffold config files, tools, and a default agent prompt.">
                <Code code={initCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="3. Run it your way: TUI, IDE/editor or headless" description="Launch the interactive TUI, run headless for scripts and CI, or expose over ACP for editor integration." border={false}>
                <Code code={runTuiCode} lang="sh" title="Interactive TUI" />
                <Code code={runAcpCode} lang="sh" title="Editor/IDE (via ACP)" />
                <Code code={runHeadlessCode} lang="sh" title="Headless" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Use any LLM, cloud or local</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Plus, you can bring your own.</p>
        </div>
        <TerminalDemo id="llm-player" src="/demos/llm-demo.cast" class="mx-auto max-w-4xl mb-12" />
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Cloud" description="Anthropic, OpenAI, OpenRouter, DeepSeek, Moonshot, Zai, LlamaCpp and more are supported out of the box.">
                <Code code={llmCloudCode} lang="json" title=".aether/settings.json" meta="ins={3-15}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Local" description="Run local models with llama.cpp or Ollama.">
                <Code code={llmLocalCode} lang="json" title=".aether/settings.json" meta="ins={4-7}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Combine models by alloying them together" description="Combine the strengths of multiple models by round-robining between them each conversation turn. You can even mix cloud and local models together." border={false}>
                <Code code={llmAlloyCode} lang="json" title=".aether/settings.json" meta="ins={4-7}" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Control every token in your agent's prompt</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Start with a blank slate and add only what you need.</p>
        </div>
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Start with a blank slate" description="Agents begin with no system prompt or tools.">
                <Code code={showPromptCode} lang="sh" title="terminal" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Use any markdown files you want" description="Add a prompts array to pull in markdown files &mdash; system instructions, agent guides, whatever you want.">
                <Code code={promptsCode} lang="json" title=".aether/settings.json" meta="ins={7}" />
                <Code code={systemPromptCode} lang="md" title="SYSTEM.md" />
                <Code code={agentsPromptCode} lang="md" title="AGENTS.md" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="View the fully rendered system prompt with a single command" description="So you don't waste tokens." border={false}>
                <Code code={showPromptCmd} lang="sh" title="terminal" />
                <Code code={showPromptOutput} lang="md" title="output" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<section class="px-16 py-20">
    <div class="mx-auto max-w-350">
        <div class="text-center mb-16">
            <h2 class="font-mono text-3xl sm:text-4xl font-bold text-white mb-3">Connect any tools you want (via MCP)</h2>
            <p class="text-gray-400 text-base sm:text-lg leading-relaxed max-w-2xl mx-auto">Aether agents get tools exclusively via MCP. Add what you need, nothing more.</p>
        </div>
        <div class="flex flex-col gap-6">
            <ToolShowcaseItem title="Batteries included MCPs" description='Aether includes several built-in MCP servers you can optionally enable. They run in a custom in-memory transport. <ul class="mt-3 space-y-1 text-gray-400 text-sm list-none pl-0"><li><code class="text-white">coding</code> — file I/O, grep, bash, and web fetch</li><li><code class="text-white">lsp</code> — language-aware completions and diagnostics</li><li><code class="text-white">skills</code> — reusable domain knowledge as slash commands</li><li><code class="text-white">subagents</code> — spawn parallel child agents with their own models and tools</li><li><code class="text-white">tasks</code> — persistent task tracking across turns</li><li><code class="text-white">survey</code> — allow the agent to ask you questions with structured output</li><li><code class="text-white">proxy</code> — prevent tool bloat via progressive discovery</li></ul>'>
                <Code code={toolsWireCode} lang="json" title=".aether/settings.json" meta="ins={7}" />
                <Code code={toolsBuiltinCode} lang="json" title="mcp.json" meta="ins={3-9}" />
            </ToolShowcaseItem>
            <ToolShowcaseItem title="Any MCP server" description="Plug in any external MCP server via stdio, SSE, or streamable HTTP. Remote server OAuth credentials are stored securely in your keychain." border={false}>
                <Code code={toolsExternalCode} lang="json" title="mcp.json" meta="ins={4-8}" />
            </ToolShowcaseItem>
        </div>
    </div>
</section>

<CallToAction title="Ready to build?" description="Use the full CLI or pick individual crates &mdash; aether-core, llm, mcp-servers, wisp, crucible &mdash; to compose your own agent.">
    <a href="/getting-started/introduction/" class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium bg-accent-600 text-white hover:bg-accent-500 transition-all duration-200 shadow-lg shadow-accent-900/30">Read the docs <span aria-hidden="true">&rarr;</span></a>
    <a href={GITHUB_URL} class="inline-flex items-center gap-2 px-6 py-3 rounded-lg font-medium border border-gray-500 text-gray-200 hover:border-accent-400 hover:text-accent-400 transition-all duration-200"><span>View on GitHub</span><svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg></a>
</CallToAction>
