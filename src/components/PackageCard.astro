---
interface Props {
    name: string;
    description: string;
}
const { name, description } = Astro.props;
---
<div class="pkg-card">
    <h4 class="font-mono text-accent-400 font-semibold mb-1">{name}</h4>
    <p class="text-gray-300 text-sm">{description}</p>
</div>
