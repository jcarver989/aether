# Create a directory structure with chained commands

Please use a single bash command with `&&` to:
1. Create a directory called `build`
2. Create a file called `build/output.txt` with the content "Build successful"
3. List the contents of the build directory

Chain these operations together with `&&`.
