Lists available skills for explicit discovery.

Call this before `get_skills` to discover which skill names are available. Use the returned `name` values exactly when calling `get_skills`.

Results are intentionally lightweight and include metadata only (`name`, `description`, `tags`). Skill bodies and `availableFiles` are only returned by `get_skills`.

## Usage

```json
{}
```
