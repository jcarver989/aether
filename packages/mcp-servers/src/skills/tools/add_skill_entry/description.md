Add an entry to a skill's agent entries section.

Creates the skill directory if it doesn't exist. Appends entries to the `## Agent Entries`
section at the bottom of SKILL.md, preserving all human-written content above.

Use `replace_id` to update an existing entry in-place (resets its score counters).